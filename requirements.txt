# Core — required to train, evaluate, and serve the recognizer.
# Version ranges (not hard pins) so this installs on modern Python, macOS/Apple
# Silicon, Linux, and CUDA setups alike. Any recent PyTorch works; it selects a
# compatible NumPy automatically.
torch>=2.2
numpy>=1.24
opencv-python-headless>=4.9
Pillow>=10.0
PyYAML>=6.0
tqdm>=4.65
matplotlib>=3.7
python-Levenshtein>=0.25    # fast edit distance for CER (pure-python fallback exists)
Flask>=3.0                  # web dashboard

# Optional — real data from HuggingFace (needs network access):
#   pip install datasets
# Optional — learned YOLO detector + detector training:
#   pip install ultralytics
